#!/usr/bin/python3
def element_at(my_list, idx):
    if idx < 0:
        return (0)
    elif idx > len(my_list):
        return (0)
    else:
        idx = idx + 1
        idx in my_list
        return (idx)
