#!/usr/bin/python3
def element_at(my_list, idx):
    if idx < 0:
        return(0)
    elif idx > len(my_list):
        return(0)
    else:
        idx in my_list
        idx = idx + 1
        return(idx)
